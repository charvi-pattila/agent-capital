# Claude Manager — Progress Log

## What We Built

A full-stack "ultimate Claude bot manager" — one dashboard to run and manage multiple Claude Code projects instead of juggling terminals.

---

## Stack
- **Backend:** Flask (Python) on port 8888
- **Frontend:** React + Vite on port 5173 (dev)
- **Session management:** tmux (one session per project)
- **Storage:** JSON files + markdown per project under `data/projects/{id}/`

---

## File Structure
```
agent-capital/
├── backend/
│   ├── server.py          # Flask API (full rewrite)
│   └── chat_runner.sh     # tmux session starter
├── frontend/
│   ├── src/
│   │   ├── App.jsx                        # Router + sidebar + Close All
│   │   ├── App.css                        # Global dark theme
│   │   ├── index.css                      # Component styles
│   │   ├── api/index.js                   # All fetch + SSE helpers
│   │   ├── pages/
│   │   │   ├── Dashboard.jsx              # Project list + new project
│   │   │   ├── Project.jsx                # Per-project view (tabs)
│   │   │   └── Running.jsx                # Active sessions
│   │   └── components/
│   │       ├── ProjectCard.jsx            # Card on dashboard
│   │       ├── NewProjectModal.jsx        # Create project modal
│   │       ├── Chat.jsx                   # Live terminal mirror
│   │       ├── MemoryPanel.jsx            # Memory hierarchy viewer/editor
│   │       ├── TestPanel.jsx              # Test runner
│   │       └── PreviewPanel.jsx           # Embedded iframe preview tab
├── data/projects/{id}/
│   ├── meta.json                          # Project metadata
│   ├── messages.json                      # Chat history (legacy, kept for context)
│   ├── memory/
│   │   ├── last_session.md               # Auto-saved on pause
│   │   ├── errors.md                     # Error log
│   │   ├── skills.md                     # Reusable patterns
│   │   └── components/{name}.md          # Per-component memory
│   └── tests/tests.json                  # Test definitions + results
└── venv/                                  # Python venv (flask, flask-cors)
```

---

## Key Behaviors Working

| Feature | Status |
|---|---|
| Dashboard with project cards | ✅ |
| Create project (emoji, name, desc, path, opening message) | ✅ |
| Start project → spins up tmux Claude session | ✅ |
| Memory context injected on Start (last session, errors, skills, components) | ✅ |
| Pause → Claude auto-saves session summary → kills tmux | ✅ |
| Chat tab = live terminal mirror (SSE, 300ms, diff-only updates) | ✅ |
| Memory tab = view/edit all memory types + add components | ✅ |
| Testing tab = add tests (description/screenshot/URL), run via Claude | ✅ |
| Running tab = shows all active sessions | ✅ |
| Vite proxy fix (127.0.0.1 not localhost, IPv4 vs IPv6 issue) | ✅ |
| Native terminal window opens on Start (`tmux attach` in Terminal.app), auto-closes on Pause/Close All (tracked by window ID, doesn't touch windows you open yourself) | ✅ |
| Close All (sidebar button) — saves memory + kills every running session in parallel, waits for the real reply to finish rather than a fixed timer | ✅ (needs UI polish, see below) |
| Native macOS notifications when a prompt finishes (chat messages, tests, auto-resume-on-start); suppressed during Pause/Close All | ✅ |
| Preview tab — per-project embedded iframe URL, editable, with Refresh/Edit/Open-in-new-tab, warns if URL is the manager itself | ✅ |
| Image paste/drag-drop in Chat tab (uploads to project dir, path sent to Claude) | ✅ |
| Close All modal with progress bar + per-project status | ✅ |
| Import existing folders as projects (scan `~/code/my-claude/`) | ✅ |
| Delete project (card hover 🗑) + search/filter on dashboard | ✅ |
| Auto-start each project's own dev/app server (`server_cmd`) alongside its Claude session | ✅ |
| Login gate — single shared password, session cookie (`backend/set_password.py` to enable) | ✅ |
| Blocked-session detection — Running tab flags a session idle >90s, with prompt-specific reasons | ✅ |
| Global memory — one note shared across all projects, injected into every session's context | ✅ |
| Mobile responsive layout — Dashboard/Project tabs verified overflow-free at 320–430px | ✅ |
| Screenshot comparison testing — headless-Chrome capture + Pillow diff, baseline/latest/diff in Testing tab | ✅ |

---

## How to Run
```bash
# Terminal 1 — backend
cd agent-capital
source venv/bin/activate
python backend/server.py

# Terminal 2 — frontend
cd agent-capital/frontend
npm run dev
```
Open: http://localhost:5173

---

## 2026-07-08 session — entire 07-07 backlog shipped

All five user-reported items built and tested. Full browser UI test suite ran same day (15/15 passed) via puppeteer-core + CDP against a scratch Chrome instance — covered dashboard cards, running badge, search, delete-with-confirm, import modal (list/uncheck/import lucky-planner), chat image paste → chip → send → upload, preview URL + self-URL warning, and the Close All modal through confirm → progress → done. Screenshots reviewed. (The Claude Chrome *extension* wouldn't connect — likely needs a claude.ai login in Chrome Profile 3 — hence the CDP route; worth fixing for future sessions.)

1. **Image/screenshot paste in Chat tab — DONE.** Paste (or drag-drop) images into the Chat input → thumbnail chips with × remove appear above the input → on Send each image uploads via new `POST /api/projects/<pid>/upload` (saved to `data/projects/{pid}/uploads/<timestamp>.<ext>`) and the message gets `[Image attached: /abs/path]` lines appended so Claude reads the file. Can send images with no text. Verified: upload endpoint (direct + through Vite proxy) and tmux delivery of the composed message (via a fake tmux session, no tokens burned). *Still open: pasting into the native Terminal.app window — separate question, untested.*
2. **Preview tab wrong URL — was indeed stale test data.** `5cab0770`'s `preview_url` reset to the real snake-and-ladders URL `http://localhost:4173` (port read from its `server.js`). Also added a guard: PreviewPanel now shows a ⚠ warning (in both edit form and a banner over the iframe) whenever the URL points at Claude Manager's own origin, so this can't silently recur.
3. **Sidebar/color polish — first pass done (user hasn't seen it yet).** Sidebar: darker panel, gradient logo text, "Workspace" section label, softer active-nav style (tinted bg instead of solid purple), live green count badge on Running, Close All pinned to bottom above a divider. Palette tweaked (deeper bg, better text contrast). If user still dislikes something, get specifics.
4. **Close All modal — DONE.** New `CloseAllModal.jsx`: confirm step listing the running projects → progress bar + per-project spinner/✓ (polls `/api/running` every 2s while `POST /api/close-all` is in flight) → done state. Replaces window.confirm + inline sidebar text.
5. **Bulk-import folders — DONE (explicit button flow).** "⤵ Import folders" button on Dashboard → `ImportModal.jsx` lists untracked folders under `~/code/my-claude/` via `GET /api/import/candidates` (skips hidden/node_modules/etc, the manager repo itself, and already-tracked paths; guesses description from package.json/README and an emoji from the name), all pre-checked with editable names → `POST /api/import` creates them. **Gotcha fixed:** the `To-Do List ` folder has a trailing space in its real name — import must not blindly `.strip()` paths (falls back to stripped only if raw path missing). "To-Do List" was imported for real as the test; the other 5 folders (lucky-planner, my-react-app, research-emailer, thatgirl, empty buget-planner) are left for the user to pick in the modal.

Bonus roadmap items done: **Delete project** (🗑 on card hover, hidden while running, confirm explains code folder is untouched) and **Search/filter projects** (header input, filters name+description).

### Same-day follow-ups (user-requested, built + tested)
- **Create folder + auto-start from New Project modal**: the project folder is now created on disk immediately at creation (`os.makedirs`), and a "Start a Claude session right away" checkbox (default ON) starts Claude + opens the Terminal window even with no opening message (`auto_start` flag on `POST /api/projects`). Tested live: folder created, tmux session up, then cleaned up.
- **Unread reply badges**: `meta.unread` increments whenever a chat reply finishes (both the `/message` watcher and `send_to_session` when notifying); `POST /api/projects/<pid>/seen` zeroes it. Project page marks seen on open + every 10s while open. Running page shows a red count badge on the project emoji + "N new replies since you looked" line (polls every 3s — note: `/api/running/stream` SSE is dead weight, backend never broadcasts to it). Sidebar Running badge turns red + pulses with total unread count when any exist (green count of running sessions otherwise).

### Older backlog — cleared 2026-07-22, see that session's note at the end of this file (chronologically last).

---

## 2026-07-20 session — Chat tab scroll, fixed for real

User reported "scroll doesn't work" repeatedly; took several rounds to find the actual cause(s) rather than guessing. In order:
1. **Modal overflow bug (real, fixed).** `.modal`/`.modal-overlay` (App.css) had no `max-height`/`overflow-y`, so on a short browser window a modal's bottom (Create/Cancel buttons) rendered off-screen with zero way to reach it. Fixed with `max-height: calc(100vh - 48px); overflow-y: auto` on `.modal` and `overflow-y: auto` on `.modal-overlay`.
2. **Chat.jsx live-pin bug (real, fixed).** `handleScroll` referenced an undeclared `atBottomRef`, and the `pauseLive()`/`resumeLive()` mechanism (described in the component's own comments) was never wired to any event, so the terminal snapped to the bottom on every SSE tick while Claude was generating — impossible to read scrollback while it was actively working. Fixed by having `handleScroll` call `pauseLive()`/`resumeLive()` based on distance from bottom.
3. **The actual root cause.** Claude Code's CLI runs inside tmux's *alternate screen buffer* (confirmed via `tmux display -p '#{alternate_on} history=#{history_size}'` → `1` / `0` on real sessions) — meaning tmux keeps **zero scrollback**, and `start_session` never set an explicit pane size, so tmux defaulted to a small ~23-row pane. The mirrored `.terminal-body` almost never had more than 23 lines to show, so there was rarely anything to scroll into, and the box looked mostly empty.
4. **Real fix.** `start_session` now creates tmux sessions at `-x 120 -y 200` and locks that size with `tmux set-option -w window-size manual` (so the native Terminal.app window attaching later — a smaller client — doesn't shrink it back down). Claude Code's TUI renders scrollback to fill whatever pane it's given, so this alone makes the mirror capture ~200 lines instead of ~23 — enough to genuinely overflow `.terminal-body` and get a **real native browser scrollbar**, filling the box properly. `Chat.jsx`'s wheel handler was simplified to a boundary trigger: normal wheel scroll is left alone (pure native scrolling, smooth, no jank); only when the user hits `scrollTop <= 0` and keeps scrolling up does it POST to the new `POST /api/projects/<pid>/scroll` endpoint (`{direction, count}`), which runs `tmux send-keys ... PageUp/PageDown` so Claude's own TUI pages back further (it says as much in its own UI: "scroll with PgUp/PgDn").
5. Already-running sessions don't get the bigger pane automatically (only new `start_session` calls do) — apply live via `tmux set-option -t claude_<pid> -w window-size manual && tmux resize-window -t claude_<pid> -x 120 -y 200` (safe, non-destructive, same as resizing any terminal).

All of it verified against a real, disposable `claude --dangerously-skip-permissions` session in a scratch directory (never the user's actual project sessions) — confirmed PageUp/PageDown genuinely scroll the CLI's transcript, confirmed the manual window-size survives a simulated smaller-client attach, and confirmed via headless Chrome that ordinary scrolling now fires zero API calls while the boundary fallback fires only when actually stuck at the top.

**Also:** repo had never been committed — initialized git, added a root `.gitignore` (excludes `venv/`, `data/` (personal chat histories/memory — not source), `.claude/settings.local.json`, pycache/logs), and made the initial commit.

---

## 2026-07-22 session — rest of the backlog (auth, blocked-detection, global memory, mobile, screenshot testing, paste verification)

Planned via Ultraplan-refined plan (`~/.claude/plans/streamed-juggling-church.md`), then built and verified locally, item by item, each against disposable scratch data/sessions rather than the user's real projects.

1. **Auth for remote access.** Single shared password (this is a personal, single-operator dashboard) hashed with `werkzeug.security` (already a Flask dependency, no new package). `backend/set_password.py` writes the hash to `data/.auth_hash` (gitignored); `server.py` checks it fresh on every request via `@app.before_request` — no password set yet ⇒ gate is skipped entirely, so a fresh checkout still runs. `/login` (minimal inline-HTML form) + `/logout` + a Flask session cookie (`data/.secret_key`, generated once, persists across restarts). Verified with Flask's `test_client()`: unauthenticated blocked (401 API / redirect browser), wrong password rejected, correct password unlocks the session, logout re-locks it, and the "no password configured" pass-through genuinely lets a fresh checkout through untouched.
2. **Blocked-session detection.** Turned out to be mostly a backend gap — `Running.jsx`'s "⚠️ Needs Your Input" UI already existed and was dead code, since nothing ever set `status: "blocked"`/`blocked_reason`. Added a `blocked_monitor()` background thread (same shape as the existing `limit_monitor`), ticking every 15s, diffing each running session's pane text (reusing the same stable-pane technique `wait_for_stable_reply` already used once per message) — idle >90s flags as blocked, with a specific reason if the idle pane text matches a known confirmation-prompt pattern (`(y/n)`, "do you want to proceed", a numbered `❯ 1.` menu), else a generic "Idle — may be waiting for input". Live/derived state only, never written to `meta.json`. Verified with a disposable fake tmux session: fresh → not blocked; idle past threshold → generic reason; fed a `(y/n)`-style line → specific quoted reason; session killed → watch entry clears.
3. **Global memory.** One free-form note (`data/global_memory/notes.md`, sibling to `data/projects/`) shared across every project — simpler than replicating the per-project last_session/errors/skills taxonomy, since "shared across all projects" is really just one running note. New `GET`/`PUT /api/global-memory`; injected as the first section of `build_context_prompt()` when non-empty. `MemoryPanel.jsx` got a new "Global Memory" card above Core Memory. Verified: PUT round-trips, shows up in `build_context_prompt` for an unrelated project, and renders correctly in the real UI (screenshotted via headless Chrome).
4. **Mobile responsive layout.** Extended the *existing* `@media (max-width: 700px)` block in `App.css` (it already handled the sidebar-as-topbar and, it turned out, the project-tabs bar) rather than adding a parallel system. Found the real breakage by CDP-driving a real headless Chrome with `Emulation.setDeviceMetricsOverride` (the Chrome extension route doesn't connect on this machine, and the plain `--screenshot` CLI flag doesn't respect `--window-size` for viewport purposes — had to script it over the DevTools protocol directly with a throwaway `ws`-based Node harness) and measuring `document.body.scrollWidth` vs `clientWidth` for actual overflow, not guessing from reading CSS. Real bug found: Dashboard's header row (search + Import + New Project) didn't wrap, so buttons wrapped their own label text mid-word. Fixed by letting `.page-header`/`.page-header-actions` wrap, plus proactive `flex-wrap` on the other `justify-content: space-between` row patterns (`.session-card`, `.test-item`, `.terminal-header`) and a `.project-grid` single-column safety net. Verified zero horizontal overflow at 320/375/430px across Dashboard and all four Project tabs, with screenshots confirming clean (not just non-overflowing) layout.
5. **Screenshot comparison testing.** `type: "screenshot"` existed only as a UI label before this — `run_tests_for_project` never branched on it. Now: captures via the system Chrome (`--headless=new --screenshot=`, no Playwright/Selenium install), diffs with Pillow (`ImageChops.difference`, ignoring small anti-aliasing deltas below a threshold), stores `baseline/latest/diff.png` under `data/projects/{pid}/tests/{test_id}/`. First run on a test saves the baseline; later runs fail past 1% differing pixels. New `GET .../tests/<id>/image/<kind>` and `POST .../tests/<id>/accept-baseline`. `TestPanel.jsx` shows the three thumbnails + diff % + an accept-baseline button for screenshot-type tests. Verified the full lifecycle end-to-end through the real HTTP API against a throwaway static test page: baseline save → pass on match → fail with correct diff % on a real content change → accept-baseline → pass again — then re-verified visually through the actual rendered UI (desktop and 375px mobile, both overflow-free) via the CDP harness.
6. **Native Terminal.app image-paste — verified, and it doesn't work.** Tested for real: a disposable `claude --dangerously-skip-permissions` session in a scratch dir, opened in an actual Terminal.app window (`osascript ... do script "tmux attach"`), clipboard set to a PNG (`set the clipboard to (read ... as «class PNGf»)`), paste simulated via System Events `keystroke "v" using command down`. Result: **nothing happens** — no text, no file-path reference, no visible effect at all. A control test with plain text on the clipboard, same window, same keystroke mechanism, landed correctly in Claude Code's input box — so the paste mechanism itself works fine; Terminal.app simply has nothing to paste when the clipboard holds only image data (no text representation), unlike iTerm2's proprietary inline-image protocol. This is a real, verified limitation, not a bug in this codebase — the documented workaround is the web Chat tab's existing paste/upload flow (shipped 2026-07-08), which already handles images correctly.

**Incident:** while reproducing a tmux cold-start race earlier this session (unrelated to items above — see [[serving-setup]] for that fix), ran `tmux kill-server` to test server-startup timing and it killed every session on the default socket, including the user's real live session, not just the scratch one being tested. No file data was lost (only in-terminal conversation state), but worth remembering: **never `tmux kill-server` without checking `tmux list-sessions` first** — use a scoped `kill-session -t <name>` or a separate `-L` socket for anything that isn't deliberately a full-server test.

**2026-07-24 follow-up — committed, still needs real-world testing.** Landed as commit `38b39bd` ("Ship remaining roadmap backlog..."), pushed to none yet (local only as of this note — ask before pushing). Backend was independently restarted by the user at some point after this session's edits and confirmed live (spot-checked `/api/global-memory` over HTTP, got a real response) — so the code above is actually running, not just committed. Separately diagnosed a "phone can't reach the server" report the same day: **not related to any of this session's changes** — the Mac's LAN IP had drifted again (192.168.255.76 → .47, same recurring DHCP issue as 2026-07-20, see [[serving-setup]]), fixed by pointing the phone at the new IP. **All six features above are freshly built and mechanically verified (scratch data, disposable sessions, real HTTP round-trips) but not yet exercised in normal day-to-day use — still up for testing**, in particular: the login flow from a real phone browser (only Flask's test client and headless Chrome have hit it so far), blocked-detection against a real Claude session that's actually stuck on a prompt (the automated test simulated idle time by rewinding timestamps rather than waiting for real elapsed time), and screenshot testing against one of the user's actual project URLs rather than a throwaway fixture page.

---

## 2026-07-25/26 session — About tab, interactive select prompts, terminal-style chat input, Council review loop

Committed as `de1dcb4` ("Add About tab, council code-review loop, and terminal-style chat input"), local only — ask before pushing. **Backend needs a restart to pick up any of this** (new routes: `generate-description`, `send-keys`, `council*`; the `/message` route also now triggers the council check).

1. **About tab.** New per-project tab (`AboutPanel.jsx`) showing/editing `description`, plus a "Generate from code" button (`POST /api/projects/<pid>/generate-description`) that shells out to `claude -p "<prompt>" --dangerously-skip-permissions` in the project's directory (one-shot, independent of the interactive session) to read the code and write a 1-2 sentence description, falling back to the existing `guess_description` heuristic (package.json/README) on failure. `PUT /api/projects/<pid>` now also accepts `description` for the manual-edit path. Verified visually via headless Chrome (edit/save round-trip); the generate button itself needs the backend restart before it's live.

2. **Interactive select-prompt chips, plus a real bug found and fixed.** Claude Code's own numbered-option prompts (`❯ 1. ...`, arrow-key navigation) render as plain text in the terminal mirror — `Chat.jsx` now parses that block (`parseSelectPrompt`, anchored on the `❯` cursor line, bounded by blank lines above/below) and renders it as clickable chips, or lets you just press the option's number key while the input is empty. Clicking/pressing computes the Up/Down delta from the parsed cursor position and POSTs to a new `POST /api/projects/<pid>/send-keys` (`{keys: [...]}`, whitelisted key names, reuses the `scroll` endpoint's tmux `send-keys` pattern). **Found via direct testing against a real, disposable `claude --dangerously-skip-permissions` session** (the actual trust-folder prompt, not a mock): bundling multiple keys into one `tmux send-keys` call with zero delay between them causes Claude Code's Ink input handler to silently drop all but the last key — `send-keys Down Enter` in one call just re-selected the already-highlighted default option instead of moving down first. Fixed by having the backend send each key as a separate `tmux send-keys` call with a ~60ms gap; re-verified against the same kind of real session that the delayed version genuinely navigates (confirmed by watching the session actually exit when "No, exit" was selected via Down+Enter).

3. **Terminal-style chat input.** Removed the Send button — Enter submits (already worked, button was redundant), with a quiet "↵" hint replacing it. This surfaced the send-keys bundling bug above, since the same underlying mechanism is used for option-picking.

4. **Council: 5 independent Claude testers auto-review every change, with an auto-fix loop.** User explicitly chose the most autonomous config when asked: (a) triggers automatically after every chat turn if the project's git working tree changed since the last check (tracked file diff + untracked-file preview, hashed into a signature so an unchanged diff is a no-op — skips entirely if the project isn't a git repo, no auto-`git init`), (b) each of the 5 testers is an independent one-shot `claude -p --dangerously-skip-permissions` call that invents its own test cases from the diff (no pre-defined test list required) and votes APPROVE/REJECT, (c) on a non-unanimous vote the rejection reasons get bundled into a message sent into the *live* interactive session asking it to fix, then the council re-runs on the new diff — capped at `COUNCIL_MAX_ATTEMPTS = 3` before giving up and just reporting. New per-project **Council tab** (`CouncilPanel.jsx`) shows live state (idle/reviewing/fixing/approved/needs-attention) and full run history with each tester's verdict+reasoning, via `GET/POST /api/projects/<pid>/council*` + an SSE stream. Safety addition: while state is `fixing` (council pasting feedback into the live tmux session), Chat.jsx locks the input with a banner, since the council and a human typing at the same time would interleave garbled input into the same session.
   - **Thoroughly unit-tested with mocked Claude calls** (a real scratch git repo, `subprocess.run` monkeypatched only for the `claude -p` calls so git itself still runs for real): diff/signature detection on both tracked and untracked changes, unanimous-approve path, idempotent no-op on an unchanged diff, the full reject→auto-fix-message→re-review→approve cycle end to end, and confirmed the 3-attempt cap genuinely stops an always-rejecting scenario rather than looping forever.
   - **Known risk, not yet mitigated:** the auto-fix step pastes directly into the same live tmux session as normal chat — the Chat-tab input lock only covers the exact window while `state === "fixing"`, so there's still a real (if narrow) race if the user starts typing right as a council run begins. Worth watching for in practice.

5. **App-close cleanup.** Requested separately: when the backend process itself is closed (Ctrl-C/SIGTERM, not just the "Close All" button), it now gracefully stops every running session first — same `_pause_project_and_save` (save handoff summary, then kill tmux) path as Close All, refactored into a shared `_close_all_sessions()` helper, wired via `signal.signal(SIGINT/SIGTERM, ...)` + `atexit` with a dedup guard. Verified with a disposable fake tmux session (not a real project) that it actually gets killed on shutdown, and that zero-running-sessions shuts down instantly with no hang.

6. **Built, then fully reverted: a 3D "office" view.** User asked for a game-like dashboard — characters per project working at desks, walking to a shared council table during review — and iterated through three attempts same session: (a) a flat CSS grid of static character icons, (b) a CSS-3D tilted "game board" with `perspective`/`rotateX` and characters that idle-wander and walk to the council table, (c) after "make it more creative... like GTA... in an office room," a full rewrite using real Three.js (added as a new dependency) — actual WebGL scene, blocky voxel-style characters, orbit-controllable camera, characters that genuinely walk (not CSS-transition) to a correctly-positioned round table. Debugging that version required standing up a *separate* headless Chrome with `--remote-debugging-port` + CDP (the installed Claude-in-Chrome extension never connects on this machine, consistent with prior sessions) since the real browser window kept getting stolen by the user's own foreground activity (their own Chrome windows, and — worth remembering for next time — screencapturing "the frontmost window" without first calling `activate` on the Chrome *application* itself, not just reordering its window list, can capture an unrelated app entirely; this happened twice, once capturing this very session's own terminal transcript and once nearly capturing unrelated personal browsing). Found and fixed two real bugs this way (council seats double-offset by `TABLE_CENTER`, scattering them off-scene; a `rotateX` counter-rotation on desk labels in the CSS version that collapsed them to invisible slivers). **After all that, the user decided they didn't like the 3D direction at all** ("delete that part of the project entirely") — `Office.jsx`, its routes/nav entry, all its CSS, and the now-unused `three` npm dependency were removed completely (bundle back down from 777KB to 221KB). The Council *feature* (item 4 above) was unaffected — it's backend logic plus a per-project tab, never depended on the Office page. **If a game-like/visual dashboard comes up again, this exact direction (CSS-3D and then full Three.js) was already tried and explicitly rejected — ask what specifically didn't land before rebuilding something similar.**

## 2026-09-05/06 session — select-prompt fix, "make it an app" (launchd + PWA + Tailscale, Electron rejected by XProtect), single-reviewer loop, context-bloat monitor

**Everything below is uncommitted** (26 files: modified backend/server.py, Chat.jsx, CouncilPanel.jsx, Project.jsx, Running.jsx, api/index.js, index.css, App.css, index.html, vite.config.js, package.json, README, .gitignore; new launchd/, scripts/, docs/, frontend/public/{manifest,icons}, frontend/src/sw.js, ContextGauge.jsx, desktop/). Backend changes need the foreground server (PID 54710 since 09-02) restarted; frontend/dist is rebuilt and already served.

1. **Fixed "I can see the multiple choice but can't select it".** Root cause verified in headless Chrome against a real AskUserQuestion dialog: auto-scroll aligned the tmux *cursor row* to the bottom, and during a menu the cursor sits mid-menu → lower options scrolled out of view *and* the "at bottom" check (<60px) failed on that programmatic scroll → view silently paused → chips computed Up/Down deltas against a frozen first question → wrong option ("Type something.") → Esc → "User declined to answer questions". Fix in `Chat.jsx`: scroll target is the last rendered line; caret decoupled from scroll target; `selectPrompt` parsed from a `live` state that updates even while paused. Verified Green → Large → Submit end-to-end. AskUserQuestion renders identically on 2.1.259 and 2.1.261.
2. **"Make it an app" split across 4 agents.** (a) *launchd*: `launchd/com.agent-capital.server.plist` + `scripts/install-service.sh` / `uninstall-service.sh`; `app.run` honors `PORT`/`HOST`/`SSL_CERT`+`SSL_KEY`; optional `backend/.env` merged into the service env; PATH now includes `/usr/sbin:/sbin` (scutil/ipconfig live there — found via a null `lan_ip` under the service). Tested on a `-test` label/port 8890: restarts <1s after kill; refuses while the foreground server holds 8888. **Not switched on** — handover is Ctrl-C the foreground server, then `./scripts/install-service.sh`. (b) *PWA*: manifest, icons (`scripts/make-icons.py`), apple meta tags, safe-area CSS, `sw.js` (never intercepts `/api/`), `GET /api/hostname`, "Install as an app" card in the About tab with the `.local` URL + QR. (c) *Tailscale*: `scripts/tailscale-https.sh` + `docs/REMOTE-ACCESS.md`; the cask install needs the user's password (root .pkg) so it's **not installed** — user must `brew install --cask tailscale`, sign in, enable MagicDNS + HTTPS certs, run the script. (d) *Electron* (`desktop/`): built, but macOS XProtect flagged the stock `Electron.app` runtime (bundle `com.github.Electron`, ad-hoc signed, zip SHA-256 matches the official 31.7.7 release) as malware and trashed it twice on launch. Recommended dropping Electron for Safari → File → Add to Dock. **User hasn't decided; `desktop/` is still on disk, never installed.**
3. **Council → single reviewer loop** (user: 5 LLM reviewers "will always have something to report… noise for no benefit"). One `claude -p` reviewer (concrete defects only, ≤3 findings, APPROVE expected); REJECT → findings pasted to the author session, which ends its reply with `REVIEW: DONE` (loop ends) or `REVIEW: AGAIN` (re-review); no marker = DONE; `REVIEW_MAX_ROUNDS = 3` backstop. `send_to_session` now returns the reply text. Also fixed: in-flight state used to persist in council.json, so a restart mid-run left a project stuck "reviewing…" forever with all later reviews skipped (Resume Rebuilder was stuck since 07-31) — now in-memory `_review_inflight`, orphaned runs marked `aborted`. Unit-tested with stubbed reviewer/session (6 scenarios + stale/concurrent).
4. **Usage-limit watchdog deleted, context-bloat monitor added** (user: upgraded plan doesn't need limits). Reads live context size from Claude Code's transcript (`~/.claude/projects/<encoded cwd>/<id>.jsonl`: last assistant `usage` input+cache tokens, or `compact_boundary.compactMetadata.postTokens` if a compaction is the latest event); picks the transcript whose first timestamp ≥ the tmux `session_created`. Bloated at `CONTEXT_TRIM_TOKENS` (120k) or ≥`CONTEXT_MAX_HOURS` (8) with ≥half. When bloated + idle + not mid-review + 30-min cooldown: checkpoint prompt → `last_session` memory, then paste `/compact <keep/drop instructions>` and wait for "Compacting…"/"esc to interrupt" to clear. Routes `GET /api/projects/<pid>/context`, `POST …/context/trim`; `ContextGauge.jsx` in the project header (+ Trim button), `Nk ctx` tag on Running. Verified live on a scratch session: 44k → checkpoint saved → compacted in 28s → reports 6.8k. `send_text`/`NOTIFY_PHONE` removed; `set_memory` now mkdirs.
5. **Finding: both live sessions (lucky-planner, Pattila Family Manager) were ended by a Close All at 19:57:06 on 09-05** — summaries saved to memory, `data/reports/daily-report-2026-09-05.pdf` written. Nothing in this session calls that endpoint; almost certainly a tap on Close All around the malware alert. Check the foreground server terminal for the `POST /api/close-all` client address if in doubt.

**Next session, in order:** decide Electron (delete `desktop/` + its README section, or keep); restart backend / hand over to launchd; optionally commit all of the above; Tailscale login; scratch conversations from testing show up in `claude --resume` for this dir (harmless).

## 2026-09-07 session — port to Linux/WSL2 so a spare Windows PC can host it

User has a spare **Windows** computer to use as an always-on server so they can code from the phone from anywhere. Decision: run the backend inside **Ubuntu on WSL2** as a systemd service; Tailscale on the Windows side with `tailscale serve --bg 8888` fronts it over HTTPS (no cert script needed; SSE passes through unbuffered). Phone and PC do **not** need the same network; the PC needs internet continuously.

Uncommitted (on top of the still-uncommitted 09-05/06 work):
1. `backend/server.py` — portable: `IS_MACOS`/`IS_WSL`; `_find_bin()` locates `claude`/`tmux` on PATH with known fallbacks (`CLAUDE_BIN`/`TMUX_BIN` env override; not `TMUX`, tmux reserves that). `open_terminal_window`/`close_terminal_window` no-op off macOS; `notify_macos` logs `[notify] …` (journal) on Linux. `/api/hostname` now returns `public_url` (env `PUBLIC_URL`, else Tailscale MagicDNS via `tailscale`/`tailscale.exe` status --json), plain hostname + `hostname -I` on Linux, and `platform`.
2. `frontend/src/components/AboutPanel.jsx` — Install card prefers `public_url` (QR + "works from anywhere over Tailscale"), falls back to `.local` on Mac, explains PUBLIC_URL on Linux. Tolerates the old backend (no `platform` field ⇒ Mac wording). `frontend/dist` rebuilt.
3. `systemd/agent-capital.service` template (Restart=always, `KillMode=process` so a backend crash-restart doesn't kill tmux/Claude sessions, TimeoutStopSec=180 for the Close-All shutdown path, EnvironmentFile=backend/.env) + `scripts/install-service-linux.sh` / `uninstall-service-linux.sh` (mirror the macOS installer: port-holder check via `ss`, curl readiness wait).
4. `scripts/setup-linux.sh` — one-shot idempotent bootstrap: apt deps, Node 22 if <18, claude native installer, venv, UI build, password prompt, `backend/.env` (HOST=0.0.0.0, PORT, PUBLIC_URL), enables systemd in `/etc/wsl.conf` (then tells the user `wsl --shutdown` and re-run), installs the service, prints the Windows checklist.
5. `docs/WINDOWS-SERVER.md` — the Windows side: `wsl --install`, powercfg never-sleep, Task Scheduler at-logon `wsl.exe -d Ubuntu --exec /bin/true` + auto sign-in (netplwiz) so Update reboots recover, Tailscale serve, phone setup, project migration note (meta.json paths point at /Users), troubleshooting (502 ⇒ localhost forwarding; mirrored networking fallback). README + REMOTE-ACCESS.md link to it.

Verified here: Flask test client on `/api/hostname` both platforms + PUBLIC_URL; Linux notify/terminal no-op paths; systemd template renders with no leftover placeholders (a comment containing `__PLACEHOLDER__` tripped the installer's guard — reworded); `bash -n` on all scripts. **Not verified: an actual WSL run** — the user is installing Ubuntu now; first real run of `setup-linux.sh` is the test. Nothing committed; Mac foreground server (stale code) still not restarted.

**Same day, later: phone redesign.** User reported "random zoom-ins" on the phone and asked for layout choices. Root cause of the zoom: iOS Safari auto-zooms focused inputs under 16px (most were 13–14px) plus double-tap zoom. Drew a design canvas (https://claude.ai/code/artifact/0d4f1c21-7915-4592-8029-63ead31d854d — pages: Layouts A–D + list/cards, Colors 1–4, Pink P1–P3). **Decisions: palette P1 "plum + dusty rose" (teal for success so it doesn't fight the pink), layout B "terminal first" (user never named a layout — B was the leading candidate, stated as an assumption), list rows for Projects on the phone, and no emojis anywhere.** Built: `components/Icon.jsx` (stroke SVG icon set + `ProjectMark` initials tile replacing `project.emoji` — the field still exists in data/backend, just never rendered; New Project modal lost its emoji picker); `App.jsx` → `Shell` inside the router so a `/project/*` route adds `.app--project` (hides the bottom bar); phone CSS lives at the **end of `index.css`** because `main.jsx` imports `index.css` after `App.css`, so phone overrides in App.css lost the cascade (bit me once: `.terminal-header{display:none}` was silently overridden). Phone: sidebar → bottom tab bar (Projects / Running / Close All), project page = slim header (back · status dot · name · gauge bar · trim · Start/Pause) + edge-to-edge terminal + **key strip** (Esc ↑ ↓ Tab Enter, "Type" focuses the input; `onPointerDown` preventDefault so taps don't dismiss the keyboard) + tabs as a scrolling pill row at the bottom. Zoom fix: inputs 16px at ≤700px, `touch-action: manipulation`. Verified with puppeteer-core mobile emulation (scratchpad `shotrig/shot.mjs`; plain `--headless --screenshot --window-size=390` is useless — macOS Chrome clamps the window to ~500px wide and crops, which looked like an overflow bug). Desktop layout unchanged apart from colours/icons. `frontend/dist` rebuilt and live. Still uncommitted.

**End of 2026-09-07 (into 09-08):** committed `40a4e51` and pushed (public repo; `desktop/` untracked). User has installed Ubuntu on the Windows PC and cloned the repo there; **Tailscale is the pending step** (Windows install + sign-in, MagicDNS/HTTPS toggles, `tailscale serve --bg 8888`, then `scripts/setup-linux.sh --public-url …`). Whether `scripts/setup-linux.sh` itself has been run on WSL yet is unconfirmed — ask first. At the user's request the Mac backend (launchd service) was stopped with `launchctl bootout` at the end of the session — the plist is still in `~/Library/LaunchAgents`, so it comes back at next login, or immediately with `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.agent-capital.server.plist` (or re-run `scripts/install-service.sh`). Stopping it closed the two idle sessions (Personal Website, Pattila Family Manager) via the normal save-summary path.
Correction: the first `launchctl bootout` SIGKILLed the backend ~20s into the graceful close (launchd's default ExitTimeOut), leaving both tmux sessions orphaned with no summaries. Added `ExitTimeOut=180` to `launchd/com.agent-capital.server.plist` (**uncommitted**), reinstalled, ran `POST /api/close-all` (both summaries saved, daily-report PDF written), then bootout again — clean. Side finding: the daily-report email fails with Gmail `535 BadCredentials` — the app password in `backend/set_email.py`'s config is stale; PDF still lands in `data/reports/`.
